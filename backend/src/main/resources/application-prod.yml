server:
  port: ${PORT:8080}

spring:
  application:
    name: hostel-mess-management
  
  datasource:
    url: ${DATABASE_URL:jdbc:postgresql://aws-1-us-east-1.pooler.supabase.com:6543/postgres?sslmode=require}
    username: ${DATABASE_USERNAME:postgres.njpqrplfmyclgqwzkpsu}
    password: ${DATABASE_PASSWORD:Hostel-mess2026}
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 10
      minimum-idle: 5
      connection-timeout: 30000
      idle-timeout: 600000
      max-lifetime: 1800000
  
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: false
        jdbc:
          lob:
            non_contextual_creation: true
  
  security:
    user:
      name: admin
      password: admin123

# JWT Configuration
jwt:
  secret: ${JWT_SECRET:hmsSecretKey2024ForHostelMessManagementSystem}
  expiration: 86400000

# Application Configuration
app:
  meal:
    cost: 60.0
    fine-per-day: 30.0
    payment-grace-days: 7
  upi:
    id: rintocherian2006@oksbi

logging:
  level:
    root: INFO
    com.hms: INFO
    org.springframework.security: WARN
