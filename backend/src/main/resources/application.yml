server:
  port: 8080

spring:
  application:
    name: hostel-mess-management
  
  datasource:
    url: jdbc:postgresql://[2406:da14:311:1501:e7f5:28c:e1c3:b0a9]:5432/postgres
    username: postgres
    password: Hostel-mess2026
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 10
      minimum-idle: 5
      connection-timeout: 30000
  
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
        jdbc:
          lob:
            non_contextual_creation: true
  
  security:
    user:
      name: admin
      password: admin123

# JWT Configuration
jwt:
  secret: hmsSecretKey2024ForHostelMessManagementSystem
  expiration: 86400000 # 24 hours

# Application Configuration
app:
  meal:
    cost: 60.0
    fine-per-day: 30.0
    payment-grace-days: 7
  upi:
    id: rintocherian2006@oksbi

logging:
  level:
    com.hms: DEBUG
    org.springframework.security: DEBUG